'''from ex111.utilidadescev import moeda, dado

p = float(input(f'Digite o preço: \033[0;33mR$\033[m'))
moeda.resumo(p, 35, 22)'''

# Solução Gustavo Guanabara

from ex111.utilidadescev import moeda, dado

p = float(input('Digite o preço: R$'))
moeda.resumo(p, 85, 15)
